apiVersion: v1
kind: Service
metadata:
  name: lab-app
  namespace: lab-app
  labels:
    app.kubernetes.io/name: lab-app
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lab-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
